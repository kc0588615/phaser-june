# VPS Docker environment variables
# Copy to .env on VPS: cp .env.vps.example .env

POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-strong-password-here
POSTGRES_DB=phaser_june

# Production: Hetzner Volume path
PGDATA_PATH=/mnt/HC_Volume_XXXXX/pgdata

# Domain for TLS certificate (must have DNS A record pointing to VPS IP)
DB_DOMAIN=db.critterconnect.org
