# Implementation Plans

001–005 generated by the improve skill on 2026-06-10 (invocation: `plan` — DB refactor for the
species-linked combat system, spec'd from the external architecture analysis at
`C:\Users\minos\.gemini\antigravity\brain\62c8b3f2-...\architecture_analysis.md`).
006–012 generated by the improve skill on 2026-06-11 (invocation: `deep` — full-repo audit
at commit `c798752`; findings vetted against the working tree, which contains the
uncommitted 001–005 work). Execute in order. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | species_combat_traits table + ETL + enemy mapper | P1 | M | — | DONE (2026-06-10, executed in working tree — uncommitted; reviewer verified all done criteria) |
| 002  | Combat nodes fight real species (wire traits into enemy gen) | P1 | S | 001 | DONE (2026-06-10, executed in working tree — uncommitted; reviewer verified call sites + typecheck) |
| 003  | Archetype-driven enemy intent patterns | P1 | S | 002 | DONE (2026-06-10, executed in working tree — uncommitted; reviewer verified wiring, labels, frozen files untouched) |
| 004  | Starting Energy 3 → 4 | P2 | S | — | DONE (2026-06-10, executed in working tree — uncommitted; reviewer verified all 4 values + typecheck) |
| 005  | Free swap only on swap-formed 4+ (fix: enemy never acted) | P1 | S | — | DONE (2026-06-11, executed in working tree — uncommitted; reviewer verified edits + cascade ordering) |
| 006  | Auth-guard /api/discoveries/migrate and /api/player/profile | P1 | S | — | DONE (2026-06-11, executed in working tree — typecheck + grep verified) |
| 007  | Patch vulnerable deps (Clerk, drizzle-orm, protobufjs, dompurify, next) | P1 | M | — | DONE (2026-06-11, typecheck/build OK; remaining audit: next/postcss + swiper force-only) |
| 008  | Vitest baseline + unit tests for pure game/domain modules | P1 | M | — | DONE (2026-06-11, npm test + typecheck OK; 6 files/33 tests) |
| 009  | Surface run-persistence failure + close combatant prefetch race | P2 | S | — | DONE (2026-06-11, typecheck + test OK; prefetch gates + journal toast) |
| 010  | Fix ESLint config + add lint script | P2 | S | — | DONE (2026-06-11, lint runs; 106 errors/64 warnings) |
| 011  | Remove dead components and dead dependencies | P3 | S | — | DONE (2026-06-11, typecheck/test OK; lint 106 errors/63 warnings) |
| 012  | Fix stale doc references in CLAUDE.md (docs/archive reorg) | P3 | S | — | DONE (2026-06-11, docs map paths verified; match battle doc added) |
| 013  | Replace violent/medical lexicon in player-facing combat copy | P3 | S | — | DONE (2026-06-11, typecheck/test OK; display-string lexicon pass) |
| 014  | Deduction-first identity: tracking re-theme, Break Camp early-guess, capture grades, clue chips | P1 | M | — | DONE (2026-07-03, typecheck/test OK; 5 step commits) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with reason) | REJECTED (with rationale)

## Dependency notes

- 001 is the DB foundation. The rest of the architecture doc (bestiary move pools,
  creature AI, status effects, damage-type resistances, node→species assignment,
  Game.ts wiring, knowledge nodes) is deliberately NOT planned yet — those layers
  consume the traits table and should be planned after 001 lands and the trait
  data is spot-checked against real species rows.
  (2026-06-11: trait data spot-checked against the live DB — 22 rows, no nulls,
  tier mapping intentional. Gate cleared; next layers can be planned.)
- 006–012 are mutually independent; run 006/007/008 first (security + the test
  baseline that everything else builds on).
- 011 and 007 both touch package.json — run them in either order, but re-run
  `npm audit` after whichever goes second.
- Any future refactor of `src/game/scenes/Game.ts` (3,320 LOC) or
  `src/contexts/ExpeditionContext.tsx` (1,362 LOC) is BLOCKED on 008's tests.
- 010's lint findings count is a baseline; 011 should not increase it.
- 2026-06-11 independent seam review of the combined 001–005 diff (Codex):
  energy accounting across cascades correct; archetype enums match
  migration/schema/mapper; no gem/threat id renames (save-compat clean).
  Its two findings were folded into 009 (resume path never prefetches
  combatants) and 013 (lexicon leaks).

## Findings considered and rejected

- Per-species hand-authored `CreatureDef` rows (arch doc's first proposal): rejected
  in favor of derived-traits table — doesn't scale past ~24 species, doc itself
  supersedes it in the "Normalized Combat Traits" section.
- Storing traits as columns on `species` directly: rejected — `species` mixes
  import-owned and curated data already; a separate ETL-owned table with cascade FK
  matches the existing `species_deduction_profiles` precedent and keeps re-derivation
  cheap.
- (2026-06-11 audit) Committed credentials in `.env.example` / `.env.local.example` /
  `.env.vps.example` (real DATABASE_URL, Cesium Ion token, POSTGRES_PASSWORD —
  git-tracked): CONFIRMED but maintainer declined a plan; handle manually.
  Rotation is still required — committed values are burned even after removal.
- "Stale closure in handleNodeObjectiveCheckpoint" (audit subagent claim): false
  positive — reads `runStateRef.current` at call time; correct ref pattern.
- "`as any` wallet mutation allows invalid keys": false positive — `if (k in w)`
  guard at ExpeditionContext.tsx:552.
- "Missing await on recursive resume fallback": false positive — async promise
  flattening is valid.
- "Orphan EventBus events incl. game-hud-updated": overstated — emitted via the
  `EVT_GAME_HUD_UPDATED` const (Game.ts:402); subagent grepped string literals only.
  A real catalog audit would be LOW value; not planned.
- Unbounded `/api/species/catalog` query: not worth doing now — species table has
  22 rows. Revisit if the catalog grows past ~1K rows.
- N+1 in `/api/runs/list` (3 queries/run): real but deferred — low traffic, small
  data; batch with joins if run history becomes slow.
- Cesium version pinning: not worth doing — lockfile already makes builds deterministic.
- swiper 12 major upgrade: deferred from plan 007 — breaking change, 4 importers;
  prototype-pollution advisory remains open until done.
- Hybrid pages/api + app/api router split (4 legacy player routes): real friction,
  deferred — migration touches live auth routes; do after 008 exists.
- Game.ts / ExpeditionContext god-file splits, useExpeditionRun overlap: deferred —
  HIGH-risk refactors blocked on 008's test baseline.
- localStorage scatter (8 call sites), console.* logging (262 sites), API error-shape
  standardization: acknowledged debt, below the leverage line this round.

## Direction backlog (grounded options, not yet planned)

- Node→species assignment for match-battle route nodes (S) — traits pipeline built
  end-to-end but route nodes never bind a species; smallest deferred layer, biggest
  educational payoff. Natural next `plan` invocation.
- Player-progress split-brain: localStorage vs playerSpeciesDiscoveries/species_cards
  (M) — fire-and-forget migration, cross-device loss risk. Interacts with plan 006.
- Album TCG Phases 2–3: GIS stamps, completion %, rarity (L) — data tables already
  populated; spec in docs/SPECIES_ALBUM_TCG_INTEGRATION_TODO.md.
- Unify leaderboards onto playerStats; retire forgeable anonymous highScores table (M).
