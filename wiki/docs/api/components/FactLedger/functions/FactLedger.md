# Function: FactLedger()

> **FactLedger**(`__namedParameters`): `Element`

Defined in: [components/FactLedger.tsx:12](https://github.com/kc0588615/phaser-june/blob/main/src/components/FactLedger.tsx#L12)

Live deduction ledger: every fact a match has spoken, filed under its family and
trait category, with the candidates it ruled out. Updates on every accepted move
so the player learns which category each fact belongs to while playing.

## Parameters

### \_\_namedParameters

#### caseState

[`CaseState`](../../../types/expedition/interfaces/CaseState.md)

#### className?

`string` = `''`

#### variant?

`"detail"` \| `"compact"` = `'detail'`

## Returns

`Element`
