# Function: currentNodeIndexForStep()

> **currentNodeIndexForStep**(`step`): `number`

Defined in: [expedition/caseFlow.ts:68](https://github.com/kc0588615/phaser-june/blob/main/src/expedition/caseFlow.ts#L68)

Which node the HUD should call current while a step is active.

## Parameters

### step

[`FlowStep`](../type-aliases/FlowStep.md)

## Returns

`number`
