# Function: encodeFieldPlateScan()

> **encodeFieldPlateScan**(`caseSeed`, `selectedFamilies`): `Buffer`

Defined in: [lib/fieldPlateScan.server.ts:21](https://github.com/kc0588615/phaser-june/blob/main/src/lib/fieldPlateScan.server.ts#L21)

Species-blind scan: the only inputs are the persisted case seed and earned signal families.

## Parameters

### caseSeed

`string`

### selectedFamilies

readonly (`"relatives"` \| `"body"` \| `"behavior"` \| `"habits"` \| `"place"`)[]

## Returns

`Buffer`
