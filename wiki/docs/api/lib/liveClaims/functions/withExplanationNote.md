# Function: withExplanationNote()

> **withExplanationNote**\<`T`\>(`fact`, `metadata`): `T` & `object`

Defined in: [lib/liveClaims.ts:122](https://github.com/kc0588615/phaser-june/blob/main/src/lib/liveClaims.ts#L122)

Attach the public explanation note to a revealed fact when its rung has one.

## Type Parameters

### T

`T` *extends* `object`

## Parameters

### fact

`T`

### metadata

`unknown`

## Returns

`T` & `object`
